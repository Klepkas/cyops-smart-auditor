function App(): JSX.Element {
  return (
    <main className="app-shell">
      <h1>Smart Contract Auditor</h1>
      <p>Bootstrap verified — Vite + React + TypeScript are wired up.</p>
    </main>
  );
}

export default App;
